#include<iostream>
using namespace std;
#define MAX 1000000
int stack[MAX];
int top=-1;
//logic for push function
void push(int data){
    if(top>=MAX-1){
        cout<<"stack is full";
        return;
    }
    top++;
    stack[top]=data;
}

void pop(){
    if(top<0){
        cout<<"stack is empty";
        return;
    }
    int x=stack[top];
    top--;
}

int peek(){
    if(top<0){
        cout<<"stack is empty";
        return -1;
    }
    
    return stack[top];
}

int main(){
    int n,x;
    cin>>n;
    for(int i=0;i<n;i++){
        cin>>x;
        push(x);
    }

    for(int i=0;i<n;i++){
        pop();
    }

    cout<<peek()<<endl;


    return 0;

}

